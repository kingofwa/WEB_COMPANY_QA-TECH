@{
    var list_service = ViewData["list_service"] as List<Web_congty.Modal.FW.Services>;
}
@section Scripts{
    <script src="~/Public/Admin/js/service.js"></script>
    <script src="~/Public/Admin/plugins/ckfinder/ckfinder.js"></script>
}
<div class="QA_warapper-content-category">
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-12">
                <div class="header-content">
                    <div class="container-fluid">
                        <div class="row mb-2">
                            <div class="menu-crud float-lg-right mt-2">
                                <ul>
                                    <li class="d-inline-block mr-2">
                                        <a id="Modal_service" class="btn btn-xs btn-success " href="#"><i class="fas fa-plus mr-1"></i> Thêm</a>
                                    </li>
                                    <li class="d-inline-block mr-2">
                                        <a class="btn btn-xs btn-warning text-white disabled" id="edit_service"><i class="fas fa-user-edit mr-1"></i> Sửa</a>
                                    </li>
                                    <li class="d-inline-block">
                                        <a class="btn btn-xs btn-danger text-white disabled" id="delete_service"><i class="fas fa-trash-alt mr-1"></i> Xóa</a>
                                    </li>
                                </ul>
                            </div>
                        </div><!-- /.col -->
                    </div><!-- /.row -->
                </div><!-- /.container-fluid -->
            </div>
            <div class="col-md-12">
                <div class="content-category">
                    <table class="table text-center table-bordered table-hover" id="myTable"> 
                        <thead>
                            <tr>
                                <th style="width:5%;">#</th>
                                <th>Tên dịch vụ</th>
                                <th>Mô tả</th>
                                <th>Dịch vụ</th>
                                <th>Đồng hành</th>
                                <th>Ghi chú</th>
                            </tr>
                        </thead>
                        <tbody id="show-body">
                            @{
                                foreach (var item in list_service)
                                {
                                    <tr id="row_@item.Id">
                                        <td>
                                            <input type="radio" name="radiocheck" class="activecheckedit" id="typehide_@item.Id" value="@item.Id" />
                                        </td>
                                        <td> @item.Services_name</td>
                                        <td>@item.Services_title</td>
                                        <td>
                                            @if (@item.Services_status == true)
                                            {
                                                <a id="color_status_@item.Id" class="btn btn-xs btn-success" onclick='Service(@item.Id)'>Kích hoạt</a>
                                            }
                                            else
                                            {
                                                <a id="color_status_@item.Id" class="btn btn-xs btn-danger" onclick='Service(@item.Id)'>Khóa</a>
                                            }
                                        </td>
                                        <td>
                                            @if (@item.Services_maketting == true)
                                            {
                                                <a id="color_status_mk_@item.Id" class="btn btn-xs btn-success" onclick='Maketting(@item.Id)'>Kích hoạt</a>
                                            }
                                            else
                                            {
                                                <a id="color_status_mk_@item.Id" class="btn btn-xs btn-danger" onclick='Maketting(@item.Id)'>Khóa</a>
                                            }
                                        </td>
                                        <td> @item.Services_note</td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div><!-- het col-md-12 category -->
        </div><!-- het row -->
    </div><!-- het col-md-12 -->
</div><!--/QA_warapper-content-category-->
@RenderPage("modal/add_service.cshtml")

